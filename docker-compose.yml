version: '3.8'

services:
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: capitalflow
  #     POSTGRES_PASSWORD: capitalflow_dev
  #     POSTGRES_DB: capitalflow
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   networks:
  #     - capitalflow-net

  # api:
  #   build: ./capitalflow-api
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     DATABASE_URL: postgresql+psycopg://capitalflow:capitalflow_dev@db:5432/capitalflow
  #     JWT_SECRET_KEY: change-this-production-secret-at-least-32-chars
  #     JWT_ALGORITHM: HS256
  #     ACCESS_TOKEN_EXPIRE_MINUTES: 15
  #     REFRESH_TOKEN_EXPIRE_DAYS: 7
  #     GOOGLE_AI_API_KEY: ${GOOGLE_AI_API_KEY:-}
  #   depends_on:
  #     - db
  #   networks:
  #     - capitalflow-net

  admin-web:
    build: ./frontend/admin-web
    ports:
      - "4000:80"
    networks:
      - capitalflow-net

  user-web:
    build: ./frontend/user-web
    ports:
      - "3010:3010"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000/api
    networks:
      - capitalflow-net


volumes:
  pgdata:

networks:
  capitalflow-net:
